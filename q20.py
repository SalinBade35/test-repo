#wapp to ask user to input an integer representing the number of roses, convert 
#the input to an integers using typecasting and then prints the integer value

n = input("entre number of roses: ")
a = int(n)
print(a)
print(type(a))